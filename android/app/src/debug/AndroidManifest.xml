<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: demo data for checking the widget and alerts on an
     emulator without a paired PC (see DemoDataReceiver). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application>
        <receiver
            android:name="com.shadelight.iausage.debug.DemoDataReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver" />
    </application>
</manifest>
