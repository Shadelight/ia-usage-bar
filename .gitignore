# Frontend / Node
node_modules/
dist/

# Rust / Tauri
target/
src-tauri/target/
src-tauri/gen/

# Entorno / secretos (nunca subir tokens ni credenciales)
.env
.env.local
.credentials.json
*.credentials.json

# Imagen fuente del icono (los iconos generados en src-tauri/icons sí se versionan)
src-tauri/app-icon.png

# Binarios compilados (se publican en Releases, no en el repo)
/Claude Bar.exe
*.exe
*.zip
releases/

# Sistema / logs
*.log
logs/
.DS_Store
Thumbs.db

# Scratch de sourcing de iconos de proveedor (el resultado curado vive en
# src/assets/providers/, que sí se versiona; ver NOTICE para la atribución)
icons.tgz
icons_extract/
slugs.md
tarball_url.txt

# Superpowers SDD scratch (ledger, briefs, review packages)
.superpowers/

# Serena MCP project cache
.serena/

# Configuración local de asistentes; no forma parte del producto.
.claude/
